ext {
    versionCode = 17
    versionName = "1.0.5"
    version = [
            minSdk          : 15,
            targetSdk       : 28,
            compileSdk      : 28,

            // Androidx
            appcompat       : "1.1.0",
            core            : "1.1.0",
    ]
    depds = [
            // AndroidX
            androidx_annotation      : "androidx.annotation:annotation:1.1.0",
            androidx_appcompat       : "androidx.appcompat:appcompat:${version['appcompat']}",
            androidx_core            : "androidx.core:core:${version['core']}",
    ]

}
